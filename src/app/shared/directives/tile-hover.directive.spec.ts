import { TileHoverDirective } from './tile-hover.directive';

describe('TileHoverDirective', () => {
  it('should create an instance', () => {
    //const directive = new TileHoverDirective();
    //expect(directive).toBeTruthy();
  });
});
