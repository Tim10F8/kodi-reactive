import { SecondsToStringPipe } from './seconds-to-string.pipe';

describe('SecondsToStringPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToStringPipe();

    expect(pipe).toBeTruthy();
  });
});
