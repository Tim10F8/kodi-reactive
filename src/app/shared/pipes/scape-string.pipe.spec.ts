import { ScapeStringPipe } from './scape-string.pipe';

describe('ScapeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ScapeStringPipe();

    expect(pipe).toBeTruthy();
  });
});
